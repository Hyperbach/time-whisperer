# Binary files
time-whisperer
timewhisperer
result
result-*
*.exe
*.dll
*.so
*.dylib

# Nix
.direnv
.pre-commit-config.yaml

# Build outputs
build/
dist/
releases/
*.dmg
*.deb
*.sha256
*.sha256.asc

# Secrets / local config — never commit (public repo safety net)
# config.json is generated at build/run and gets rewritten with the local
# Upwork logs path; *.asc covers any exported GPG keys.
config.json
*.asc

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Logs and temporary files
*.log
*.tmp
.DS_Store

# Dependency directories
vendor/

# Rust
target/
Cargo.lock.bak

# Go (legacy)
go.work
time-whisperer.app
.idea

time-whisperer-backend
work.md

.private-keys.md
toast-stacker
safe-break-buddy
